//pop removes the element at the last index, the other elements indices are unchanged

const array = ["a", "b", "c"];

let lastElment = array.pop();

console.log(lastElment); //"c"
console.log(array);	//["a", "b"]